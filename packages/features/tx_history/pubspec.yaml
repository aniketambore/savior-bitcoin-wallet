name: tx_history
publish_to: 'none'

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  component_library:
    path: ../../component_library
  wallet_repository:
    path: ../../wallet_repository
  domain_models:
    path: ../../domain_models
  converter:
    path: ../../converter
  flutter_bloc: ^8.1.2
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
